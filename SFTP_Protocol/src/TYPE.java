/*TYPE class serves the TYPE command requested to the
 * the server, there are 3 parameters A,B and C
 * A - ASCII -> Read the bytes from the file in the system*/
/*ASCII A -> only byte data so discard the end of the word if not enough bits*/
/*ASCII B -> Binary */

public class TYPE {

}
